version: 2.1
jobs:
  Build:
    docker:
      - image: ubuntu:latest
    steps:
      - checkout
      - run: 
          name: Build-repo
          command: |
            ls     
  Test:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - run: 
          name: Deploy GitPages
          command: |
            git config --global user.email "Sockshead@users.noreply.github.com"
            git config --global user.name "Sockshead"
            echo "machine github.com login Sockshead password $GITHUB_TOKEN" > ~/.netrc
            cd website && yarn install && GIT_USER=Sockshead yarn run publish-gh-pages
workflows:
 version: 2
 HTML-dev-flow:
   jobs:
     - Build
     - Hold-For-Approval:
         type: approval
         requires:
           - Build
     - Test:
         requires:
           - Hold-For-Approval
